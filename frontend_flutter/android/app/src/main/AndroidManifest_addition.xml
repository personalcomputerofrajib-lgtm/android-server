<!-- Add this inside <manifest> tag in your real AndroidManifest.xml -->
<uses-permission android:name="android.permission.INTERNET"/>
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

<!-- For connecting to localhost on Android emulator, use 10.0.2.2 instead of 127.0.0.1 -->
